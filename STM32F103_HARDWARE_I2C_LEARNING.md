# STM32F103 硬件 I2C 问题学习笔记

## 1. 本次问题背景

本工程使用 STM32F103C8：

- OLED 使用 I2C1：PB6 为 SCL，PB7 为 SDA。
- MPU6050 使用 I2C2：PB10 为 SCL，PB11 为 SDA。
- MPU6050 总线速度为 100 kHz。
- MPU6050 的 7 位地址通常为 `0x68`，AD0 拉高时为 `0x69`。

使用硬件 I2C 后，可能出现以下现象：

- OLED 显示 `MPU:OFF`。
- `WHO_AM_I` 读取失败。
- 第一次通信正常，之后突然离线。
- 程序停在等待 I2C 标志位的循环中。
- I2C 的 `BUSY` 标志一直不清除。
- 复位 MCU 后仍然不能通信，重新断电后偶尔恢复。

这些现象不能全部简单归结为“STM32F103 的 I2C 有 Bug”。实际需要区分三类问题：

1. STM32F1 硬件 I2C 的已知限制和芯片勘误。
2. 驱动程序没有严格遵守 STM32F1 的收发顺序。
3. 上拉、电源、接线或从机异常导致总线被占用。

---

## 2. STM32F103 硬件 I2C 为什么容易卡死

### 2.1 无限等待硬件标志

最危险的写法是：

```c
while (I2C_GetFlagStatus(I2C2, I2C_FLAG_BUSY) == SET)
{
}
```

如果 SDA 被从机拉低、总线受到干扰，或者 I2C 外设状态异常，`BUSY` 可能一直保持置位，CPU 就会永远停在循环中。

同样的问题也会发生在等待以下标志时：

- `SB`：起始条件已发送。
- `ADDR`：地址阶段完成。
- `TXE`：发送数据寄存器为空。
- `BTF`：字节传输完成。
- `RXNE`：接收数据寄存器非空。

因此，所有等待循环都必须有超时。

```c
timeout = I2C_TIMEOUT;

while (I2C_CheckEvent(I2C2, event) != SUCCESS)
{
    if (timeout-- == 0)
    {
        return 0;
    }
}
```

超时只能防止 CPU 永久卡死。超时发生后，还需要清除错误并恢复总线。

### 2.2 BUSY 标志异常

STM32F1 系列硬件 I2C 存在与模拟滤波器和总线电平有关的已知限制。在上电、复位或总线干扰后，I2C 外设可能错误地认为 SDA 处于低电平，使 `BUSY` 一直置位。

常用恢复方法：

1. 禁用 I2C 外设。
2. 将 SCL、SDA 临时配置为开漏 GPIO。
3. 释放 SCL 和 SDA，使它们由上拉电阻拉高。
4. 如果 SDA 仍为低电平，输出最多 9 个 SCL 脉冲。
5. 手动产生一次 STOP：SCL 为高电平时，让 SDA 从低变高。
6. 对 I2C 外设执行软件复位 `SWRST`。
7. 重新初始化 GPIO 和 I2C。

9 个时钟脉冲的作用是让可能停在发送过程中的从机继续移位，直到释放 SDA。

### 2.3 从机导致总线被占用

如果主机在一次读取过程中复位，而 MPU6050 仍认为传输没有结束，从机可能继续拉低 SDA。此时仅复位 MCU 不一定能恢复，因为传感器没有同步复位。

可能的触发条件：

- 通信过程中按下复位键。
- 调试器在 I2C 传输中打断点。
- 从机供电不稳定。
- SCL 或 SDA 接触不良。
- 上拉电阻过大，信号上升沿太慢。
- 主从设备没有共地。

---

## 3. STM32F1 接收时序的特殊之处

STM32F1 的硬件 I2C 对最后几个字节的处理顺序要求严格。不能对所有长度都使用同一个简单循环。

### 3.1 ADDR 标志如何清除

主机发送从机地址并收到 ACK 后，硬件置位 `ADDR`。

清除 `ADDR` 的顺序是：

```c
(void)I2C2->SR1;
(void)I2C2->SR2;
```

标准库的 `I2C_CheckEvent()` 内部也会读取状态寄存器。因此，在单字节和双字节接收中，如果过早调用 `I2C_CheckEvent(I2C_EVENT_MASTER_RECEIVER_MODE_SELECTED)`，可能提前清除 `ADDR`，导致之后设置 ACK、POS、STOP 已经太晚。

### 3.2 接收 1 字节

正确思路：

1. 地址发送后等待 `ADDR` 置位，但暂时不要清除它。
2. 关闭 ACK。
3. 读取 SR1、SR2，清除 `ADDR`。
4. 立即产生 STOP。
5. 等待 `RXNE`。
6. 读取数据。
7. STOP 发送完成后恢复 ACK。

关键点是：**关闭 ACK 必须发生在清除 ADDR 之前。**

本工程读取 `WHO_AM_I` 时就是单字节接收，因此这个时序直接决定初始化能否成功。

### 3.3 接收 2 字节

正确思路：

1. 在地址阶段之前设置 `POS`，使 NACK 作用于下一个字节。
2. 等待 `ADDR`。
3. 清除 `ADDR` 并关闭 ACK。
4. 等待 `BTF`，表示两个字节已经就绪。
5. 产生 STOP。
6. 连续读取两个字节。
7. 恢复 ACK，并将 POS 恢复为默认状态。

双字节时序与单字节不同，不能只在“最后一次循环”中关闭 ACK。

### 3.4 接收 3 字节及以上

前面的字节可以按 `RXNE` 正常读取，但最后 3 个字节需要特殊处理：

1. 剩余 3 字节时等待 `BTF`。
2. 关闭 ACK。
3. 读取倒数第 3 个字节。
4. 再次等待 `BTF`。
5. 产生 STOP。
6. 读取最后两个字节。
7. STOP 完成后恢复 ACK。

本工程从 `0x3B` 开始连续读取 14 字节的加速度、温度和陀螺仪数据，因此必须正确处理最后 3 个字节。

---

## 4. 本次驱动中发现的问题

### 4.1 单字节读取顺序错误

原来的代码先通过 `I2C_CheckEvent()` 清除了 `ADDR`，然后才在循环末尾关闭 ACK 并产生 STOP。

这不符合 STM32F1 的单字节接收要求，可能造成：

- `WHO_AM_I` 读取失败。
- 多接收一个字节。
- RXNE 或 BUSY 状态异常。
- 后续通信失败。

修复位置：

```text
User/mpu6050.c
MPU6050_ReadRegs()
```

### 4.2 地址失败后没有及时处理错误

MPU6050 地址 `0x68` 无响应时，I2C 会产生应答失败 `AF`。

如果不清除 `AF`，直接尝试地址 `0x69`，第二次探测也可能失败。驱动现在会检查并清除：

- `BERR`：总线错误。
- `ARLO`：仲裁丢失。
- `AF`：应答失败。
- `OVR`：溢出或欠载。

### 4.3 BUSY 超时后只返回，没有恢复总线

仅仅从函数返回不能解除 SDA 被拉低或外设内部状态异常。

本次增加了：

- 开漏 GPIO 总线恢复。
- 最多 9 个 SCL 脉冲。
- 手动 STOP。
- I2C 软件复位。
- I2C 重新初始化。

### 4.4 初始化寄存器写入结果被忽略

MPU6050 初始化需要写入：

- `PWR_MGMT_1`
- `SMPLRT_DIV`
- `CONFIG`
- `GYRO_CONFIG`
- `ACCEL_CONFIG`

如果其中一次写入失败，原程序仍会开始校准，容易把“初始化失败”误认为“传感器正在工作”。

现在只有全部写入成功后才进入校准状态。

---

## 5. I2C 地址格式容易混淆

MPU6050 数据手册中的地址 `0x68` 和 `0x69` 是 7 位地址。

在线路上传输时，需要将 7 位地址左移一位，并在最低位加入读写位：

| AD0 | 7 位地址 | 写地址字节 | 读地址字节 |
| --- | --- | --- | --- |
| 0 | `0x68` | `0xD0` | `0xD1` |
| 1 | `0x69` | `0xD2` | `0xD3` |

STM32 标准外设库的调用方式是：

```c
I2C_Send7bitAddress(I2C2, 0xD0, I2C_Direction_Transmitter);
I2C_Send7bitAddress(I2C2, 0xD0, I2C_Direction_Receiver);
```

这里传入左移后的基础地址 `0xD0`。标准库会根据方向参数设置或清除最低位，因此不需要分别保存 `0xD0` 和 `0xD1`。

不同 HAL、LL、Arduino 或 Linux API 对地址参数的要求可能不同。移植代码时必须确认接口需要：

- 7 位地址 `0x68`，还是
- 左移后的地址字节 `0xD0`。

---

## 6. 硬件设计注意事项

### 6.1 必须使用上拉电阻

I2C 的 SCL 和 SDA 是开漏信号，设备只能主动拉低，不能主动输出高电平，必须依靠上拉电阻恢复高电平。

常见选择：

- 100 kHz、短连线：通常使用 `4.7kΩ`。
- 总线电容较大或速度较高：可考虑 `2.2kΩ` 至 `3.3kΩ`。
- 电阻过大：上升沿慢，容易产生通信错误。
- 电阻过小：低电平时电流增大。

上拉电阻应接到与 MCU IO 电平兼容的 `3.3V`。

### 6.2 模块供电和电平

需要确认：

- MPU6050 与 STM32 共地。
- PB10 接 SCL，PB11 接 SDA。
- 模块供电符合模块板设计。
- SCL、SDA 不应被上拉到超过 STM32 IO 允许范围的电压。
- AD0 状态稳定，不能悬空到容易受干扰的程度。

### 6.3 使用示波器或逻辑分析仪

建议观察：

- 空闲时 SCL、SDA 是否都为高电平。
- START 后是否发出了正确地址。
- 从机是否在第 9 个时钟产生 ACK。
- 数据阶段是否有异常拉低。
- STOP 是否真实产生。
- 信号上升沿是否过慢。

如果地址发出后始终 NACK，优先检查地址、接线、供电和上拉，而不是先修改姿态算法。

---

## 7. 推荐的软件保护结构

一个可靠的 STM32F103 I2C 驱动至少应包括：

### 7.1 所有等待均有超时

```c
if (I2C_WaitEventWithTimeout(event) == 0)
{
    I2C_Abort();
    return 0;
}
```

### 7.2 等待过程中检查错误标志

不要只等待目标事件，也要检查：

```c
I2C_SR1_BERR
I2C_SR1_ARLO
I2C_SR1_AF
I2C_SR1_OVR
```

### 7.3 失败后统一退出

统一退出函数应至少完成：

1. 尝试产生 STOP。
2. 恢复 ACK。
3. 恢复 POS。
4. 清除错误标志。

如果 BUSY 仍不释放，则进一步执行总线恢复和外设重初始化。

### 7.4 区分离线和校准状态

本工程使用：

- `MPU6050_STATUS_OFFLINE`
- `MPU6050_STATUS_CALIBRATING`
- `MPU6050_STATUS_READY`

这样可以区分：

- 传感器没有响应。
- 传感器正在校准。
- 传感器已经正常工作。

---

## 8. 故障排查顺序

当 OLED 显示 `MPU:OFF` 时，建议按以下顺序排查。

### 第一步：检查硬件

- VCC 是否正确。
- 是否共地。
- PB10、PB11 是否接反。
- SCL、SDA 空闲时是否为高电平。
- 是否存在 3.3V 上拉电阻。
- AD0 状态是否确定。

### 第二步：确认地址

读取 `WHO_AM_I`：

- AD0 为低时通常应返回 `0x68`。
- AD0 为高时通常应返回 `0x69`。

### 第三步：观察 I2C 标志

使用调试器暂停时查看：

- `I2C2->SR1`
- `I2C2->SR2`
- `I2C2->CR1`

重点判断：

- `BUSY` 是否一直为 1。
- `AF` 是否置位。
- 是否停在等待 `ADDR`、`RXNE` 或 `BTF`。

### 第四步：观察波形

- 没有 START：检查 BUSY 和外设初始化。
- 有地址但没有 ACK：检查地址、供电和接线。
- 数据读取到一半停止：检查 ACK、POS、BTF 和最后 3 字节时序。
- SDA 一直低：执行总线恢复，并检查从机状态。

### 第五步：确认程序没有无限等待

搜索所有 I2C 等待循环，确认每处都有超时和失败返回。

---

## 9. 硬件 I2C 还是软件 I2C

STM32F103 硬件 I2C 并非不能使用。只要正确处理特殊接收时序、错误标志、超时和总线恢复，通常可以稳定工作。

硬件 I2C 的优点：

- CPU 占用较低。
- 速度和时序由外设控制。
- 适合 DMA、中断和较高吞吐量。

软件 I2C 的优点：

- 状态机简单直观。
- 对 STM32F1 硬件 I2C 勘误不敏感。
- 异常时更容易完全控制 SCL 和 SDA。

选择建议：

- 学习协议、设备较少、速度要求不高：软件 I2C 更容易调试。
- 需要规范外设驱动、较高吞吐量或中断/DMA：可以使用硬件 I2C，但必须做好恢复机制。

---

## 10. 本次学习结论

1. “STM32F103 硬件 I2C 会卡死”有事实依据，但卡死通常由芯片限制、错误时序和缺少恢复机制共同造成。
2. 任何硬件标志等待都不能无限循环。
3. STM32F1 的单字节、双字节和多字节接收必须分别处理。
4. `ADDR` 的清除时机非常关键，不能在配置 ACK、POS、STOP 前被意外清除。
5. 地址 NACK 后必须处理 `AF` 等错误标志。
6. BUSY 卡住时，仅返回错误不够，还要恢复 SCL/SDA 并重新初始化外设。
7. I2C 的第一调试目标应是稳定读取 `WHO_AM_I`，之后再调试校准和姿态解算。
8. 上拉、电源、共地和波形质量与软件时序同样重要。

## 11. 相关资料

- ST RM0008：《STM32F101xx、STM32F102xx、STM32F103xx、STM32F105xx 和 STM32F107xx 参考手册》
- ST AN2824：《STM32F10x I2C optimized examples》
- STM32F101x8/B、STM32F103x8/B 芯片勘误手册
- MPU-6000/MPU-6050 Register Map and Descriptions
- 本工程驱动：`User/mpu6050.c`

