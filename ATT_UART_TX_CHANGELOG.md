# ATT 协议与 UART TX 环形缓冲改动日志

日期：2026-06-09

## 改动目标

本次改动解决两个问题：

1. 将姿态帧改为无小数点的定点整数格式，降低 STM32 字符串转换和串口传输开销。
2. 将 USART1 发送方式从逐字节阻塞等待改为 TXE 中断驱动的环形缓冲，避免姿态输出长时间占用主循环。

## ATT 协议

旧格式：

```text
ATT,12.30,-3.20,45.80
```

新格式：

```text
ATT,123,-45,876
```

字段定义：

```text
ATT,roll10,pitch10,yaw10
```

- 三个角度字段均为有符号十进制整数。
- 单位为 `0.1°`。
- `123` 表示 `12.3°`，`-45` 表示 `-4.5°`。
- 帧结尾保持 `\r\n`。
- 发送周期保持 `50 ms`，约为 `20 Hz`。
- 状态帧 `STA,CAL` 和 `STA,OFF` 格式不变。

## STM32 发送流程

新增：

- `SYSTEM/usart/uart_tx.c`
- `SYSTEM/usart/uart_tx.h`

环形缓冲参数：

- 缓冲区大小：256 字节。
- 实际可用容量：255 字节。
- 使用 `head` 和 `tail` 管理读写位置。
- 主循环负责将完整数据块写入缓冲。
- USART1 TXE 中断负责逐字节写入数据寄存器。
- 缓冲为空时自动关闭 TXE 中断。
- 空间不足时整块拒绝写入，不产生残缺帧。
- `drop_count` 用于记录入队失败次数。

`printf` 的 `fputc()` 也已接入环形缓冲，并为协议数据保留 32 字节空间，降低调试文本挤占姿态帧空间的风险。

## 姿态帧组装

`User/app_attitude_stream.c` 不再直接等待 USART1 发送完成。

当前流程：

1. 在局部数组中组装完整 ATT 帧。
2. 直接写入 `MPU6050_GetRoll10()`、`GetPitch10()` 和 `GetYaw10()` 返回的定点整数。
3. 调用 `UartTx_TryWrite()` 将整帧一次性放入 TX 环形缓冲。
4. 由 USART1 TXE 中断在后台完成发送。

## 上位机修改

`mpu_viewer/protocol.py` 现在只接受整数 ATT 字段，并按以下方式恢复角度：

```text
实际角度 = 接收整数 / 10.0
```

演示数据、上位机 README 和网页协议提示已同步到新格式。

## 工程配置

Keil 工程 `Project/led.uvprojx` 已加入：

- `SYSTEM/usart/uart_tx.c`
- `SYSTEM/usart/uart_tx.h`

`.gitignore` 增加了 Python `__pycache__` 和 `.pyc` 忽略规则。

## 验证结果

- Keil MDK 5.43 / ARMCC 5.06 完整构建通过。
- 构建结果：`0 Error(s), 0 Warning(s)`。
- TX 环形缓冲主机测试全部通过，包括：
  - 基本入队和发送顺序。
  - TXE 忙状态处理。
  - 调试输出预留空间。
  - 空间不足时整帧拒绝。
  - 环形缓冲跨尾部回绕。
- 上位机协议测试通过：

```text
ATT,123,-45,876
```

解析结果：

```text
Roll  = 12.3°
Pitch = -4.5°
Yaw   = 87.6°
```

## 兼容性说明

旧版上位机如果仍按浮点字符串直接读取，会把 `123` 解释成 `123°`。固件和上位机必须同时更新到本次协议版本。
