# MPU6050 接入与 OLED 菜单设计问题表

这份问题表基于当前 STM32F103 工程，用来梳理 MPU6050 三维角度传感器接入、OLED 显示、摇杆交互、状态变量和非阻塞任务设计。

## 1. 输入模块

### 使用哪个输入模块？

使用 5D 摇杆作为人工输入模块。

当前使用的摇杆按键：

- 上键
- 下键
- 左键
- 右键
- 中键按压

### 用哪些引脚？

| 摇杆信号 | STM32F103 引脚 |
| --- | --- |
| UP | PA2 |
| DOWN | PA3 |
| LEFT | PA4 |
| RIGHT | PB5 |
| PRESS / MID | PB1 |
| COM | GND |

### 按下是高电平还是低电平？

当前代码使用 GPIO 上拉输入。

- 未按下：高电平
- 按下：低电平

### 要不要消抖？

需要消抖。

机械按键按下和松开时会产生短时间抖动，如果不消抖，程序可能把一次按键识别成多次按键。当前由 `joystick.c` 负责扫描按键并生成稳定的按键事件。

### 为什么不用 MPU6050 做输入模块？

MPU6050 是姿态传感器，负责采集角度数据，不适合作为菜单输入。菜单输入仍然由摇杆完成，MPU6050 数据只用于显示和状态判断。

### MPU6050 输入数据从哪里来？

MPU6050 通过 I2C 通信读取：

- 加速度 X/Y/Z
- 陀螺仪 X/Y/Z
- 内部温度
- WHO_AM_I 芯片 ID

当前使用 `PB10=SCL`、`PB11=SDA`，由 `mpu6050.c` 使用软件 I2C 读取。

## 2. 输出模块

### OLED 显示哪些页面？

当前 OLED 显示 4 类页面：

| 页面 | 内容 |
| --- | --- |
| 主菜单页面 | MPU 状态、Roll/Pitch/Yaw 摘要、光照、AO、温度、菜单 |
| STATUS 页面 | 光照、AO、温度、MPU 状态、Roll/Pitch |
| SET TH 页面 | 光敏 AO、光敏阈值、左右键调节提示 |
| TRAFFIC 页面 | 交通灯模式选择 |
| MPU6050 页面 | MPU 状态、Roll、Pitch、Yaw、内部温度、芯片 ID |

### 菜单有几个选项？

主菜单有 4 个选项：

- `STATUS`
- `SET TH`
- `TRAFFIC`
- `MPU6050`

### 光标怎么表示？

光标使用字符 `>` 表示。

例如：

```text
>STATUS
 SET TH
 TRAFFIC
 MPU6050
```

当前选中的菜单项前面显示 `>`，未选中的菜单项前面显示空格。

### 光标要不要闪烁？

需要闪烁。

当前通过定时变量控制，每 500 ms 切换一次显示/隐藏状态。

### OLED 状态怎么显示 MPU6050 是否正常？

使用短状态文本显示：

| 状态 | OLED 显示 |
| --- | --- |
| 正常读取 | `OK` |
| 正在校准 | `CAL` |
| 未检测到或通信失败 | `OFF` |

### OLED 上角度怎么显示？

使用带符号的角度显示：

```text
R:+012
P:-003
Y:+045
```

MPU6050 详情页会显示 1 位小数：

```text
ROLL:+012.3
PITCH:-003.5
YAW:+045.0
```

## 3. 状态变量

### 当前页面用什么变量？

使用 `s_screen` 保存当前页面。

当前页面编号：

| 变量值 | 页面 |
| --- | --- |
| `UI_SCREEN_MENU` | 主菜单 |
| `UI_SCREEN_STATUS` | 状态页 |
| `UI_SCREEN_THRESHOLD` | 阈值设置页 |
| `UI_SCREEN_TRAFFIC` | 交通灯页 |
| `UI_SCREEN_MPU` | MPU6050 页 |

### 当前选中项用什么变量？

主菜单当前选中项使用：

```c
static uint8_t s_menu_index;
```

交通灯页面当前选中项使用：

```c
static uint8_t s_traffic_index;
```

### 是否需要刷新 OLED 用什么变量？

需要。

当前使用 3 个刷新标志：

| 变量 | 作用 |
| --- | --- |
| `s_screen_dirty` | 页面切换后需要整页重绘 |
| `s_status_dirty` | 状态数据变化或定时到，需要刷新数据区域 |
| `s_cursor_dirty` | 光标位置变化或闪烁状态变化，需要刷新菜单光标 |

### 光标显示/隐藏用什么变量？

使用：

```c
static uint8_t s_cursor_visible;
```

它决定当前光标是否显示。

### MPU6050 状态用什么变量？

在 `mpu6050.c` 中使用：

```c
static uint8_t s_status;
```

状态值：

| 状态 | 含义 |
| --- | --- |
| `MPU6050_STATUS_OFFLINE` | 离线或通信失败 |
| `MPU6050_STATUS_CALIBRATING` | 正在校准 |
| `MPU6050_STATUS_READY` | 正常工作 |

### MPU6050 角度用什么变量？

在 `mpu6050.c` 中使用：

```c
static float s_roll;
static float s_pitch;
static float s_yaw;
```

对外提供整数接口：

```c
MPU6050_GetRoll10();
MPU6050_GetPitch10();
MPU6050_GetYaw10();
```

返回值单位是 0.1 度。例如返回 `123` 表示 `12.3` 度。

## 4. 事件

### 上键事件做什么？

在主菜单：

- 选中项上移

在交通灯页面：

- 交通灯模式选项上移

在其他页面：

- 当前没有使用

### 下键事件做什么？

在主菜单：

- 选中项下移

在交通灯页面：

- 交通灯模式选项下移

在其他页面：

- 当前没有使用

### 左键事件做什么？

在 STATUS 页面：

- 返回主菜单

在 TRAFFIC 页面：

- 返回主菜单

在 MPU6050 页面：

- 返回主菜单

在 SET TH 页面：

- 光敏阈值减少 50

### 右键事件做什么？

在 SET TH 页面：

- 光敏阈值增加 50

在其他页面：

- 当前没有使用

### 按压事件做什么？

在主菜单：

- 进入当前选中的页面

在 STATUS 页面：

- 返回主菜单

在 SET TH 页面：

- 保存当前阈值并返回主菜单

在 TRAFFIC 页面：

- 应用当前选中的交通灯模式

在 MPU6050 页面：

- 返回主菜单

### 为什么事件要和按键状态分开？

按键状态表示“现在是否按着”，事件表示“刚刚发生了一次有效动作”。

菜单操作应该使用事件，否则用户长按一下可能会连续触发很多次。

## 5. 文件划分

### `joystick.c` 负责什么？

负责摇杆底层输入：

- 初始化 GPIO
- 扫描上/下/左/右/按压
- 按键消抖
- 生成按键事件
- 提供 `Joystick_GetEvents()` 给应用层读取

它不应该关心当前 OLED 页面，也不应该直接修改菜单。

### `app_ui.c` 负责什么？

负责 OLED 用户界面：

- 当前页面管理
- 菜单选中项管理
- 摇杆事件处理
- 页面切换
- OLED 绘制
- 光标闪烁
- 定时刷新状态数据

它通过其他模块的接口读取数据，例如：

- `App_Light_GetAO()`
- `App_Temp_GetTemp10()`
- `MPU6050_GetRoll10()`

### `mpu6050.c` 负责什么？

负责 MPU6050 传感器：

- 软件 I2C 通信
- 检测 WHO_AM_I
- 初始化 MPU6050 寄存器
- 陀螺仪零偏校准
- 周期读取加速度和陀螺仪
- 计算 Roll/Pitch/Yaw
- 提供状态和角度读取接口

### `main.c` 负责什么？

负责系统启动和任务调度：

- 初始化延时、定时器、串口、传感器、OLED、摇杆
- 启动 MPU6050 校准
- 在 `while (1)` 中轮询各个任务

主循环中不直接写复杂业务逻辑，只调用各模块的 `Task()`。

### `README.md` 负责什么？

负责说明工程使用方法：

- 硬件接线
- OLED 页面
- 菜单操作
- 串口命令
- 调试检查
- 文件位置

## 6. 非阻塞

### 哪里不能 `delay`？

主循环里的任务函数不能长时间 `delay`。

尤其是这些函数里不应该长时间阻塞：

- `App_Light_Task()`
- `App_Temp_Task()`
- `MPU6050_Task()`
- `Joystick_Task()`
- `App_UI_Task()`
- `App_UARTPractice_Task()`

原因：

- 会导致按键不灵敏
- 会导致 OLED 刷新卡顿
- 会导致串口命令处理变慢
- 会影响传感器周期采样

### 哪些地方可以短暂 `delay`？

初始化阶段可以适当 `delay`。

例如：

- OLED 上电初始化
- MPU6050 上电等待
- MPU6050 启动校准

这些发生在进入主循环之前，不会影响运行时交互。

### 刷新 OLED 的间隔怎么控制？

使用系统毫秒计时 `Timing_GetTick()`。

当前状态刷新周期：

```c
#define UI_STATUS_REFRESH_MS 500
```

逻辑：

```c
if (now - s_last_status_time >= UI_STATUS_REFRESH_MS)
{
    s_last_status_time = now;
    s_status_dirty = 1;
}
```

这样不需要 `delay_ms(500)`，主循环仍然可以继续运行。

### 光标闪烁怎么控制？

使用独立的闪烁计时变量。

当前光标闪烁周期：

```c
#define UI_CURSOR_BLINK_MS 500
```

逻辑：

```c
if (now - s_last_cursor_time >= UI_CURSOR_BLINK_MS)
{
    s_last_cursor_time = now;
    s_cursor_visible = !s_cursor_visible;
    s_cursor_dirty = 1;
}
```

### MPU6050 采样怎么做到非阻塞？

`MPU6050_Task()` 使用时间间隔判断。

当前采样周期：

```c
#define MPU6050_TASK_MS 20
```

只有到达 20 ms 才读取一次数据，没有到时间就直接返回。

### 为什么 OLED 不每次都整屏刷新？

整屏刷新比较慢，会占用 I2C 时间。

当前使用刷新标志分区刷新：

- 页面切换：整屏刷新
- 数据变化：只刷新数据区
- 光标变化：只刷新菜单行

这样界面更流畅，按键响应也更快。

## 7. MPU6050 接入问题

### 为什么 MPU6050 不接 OLED 同一组 I2C？

OLED 已经使用硬件 I2C1：

- PB6：SCL
- PB7：SDA

为了减少对现有 OLED 驱动的影响，本次 MPU6050 使用剩余引脚：

- PB10：SCL
- PB11：SDA

并采用软件 I2C 独立通信。

### MPU6050 的地址是多少？

MPU6050 常见地址：

| AD0 电平 | 7 位地址 | 代码使用的 8 位写地址 |
| --- | --- | --- |
| 低电平 | 0x68 | 0xD0 |
| 高电平 | 0x69 | 0xD2 |

当前代码会先尝试 0x68，如果失败再尝试 0x69。

### 上电为什么要保持静止？

启动时要采样陀螺仪零偏。

如果校准时模块在移动，程序会把移动产生的角速度当成零偏，后续角度会明显漂移。

### 为什么 Yaw 会漂移？

MPU6050 没有磁力计。

Yaw 只能靠陀螺仪积分计算，陀螺仪存在零偏和噪声，所以时间长了会漂移。

Roll 和 Pitch 可以用加速度计辅助修正，所以更稳定。

## 8. 调试问题

### OLED 显示 `MPU:OFF` 怎么办？

检查：

- VCC 是否接 3.3V
- GND 是否共地
- SCL 是否接 PB10
- SDA 是否接 PB11
- 模块是否有 I2C 上拉电阻
- AD0 是否接错
- 焊接或杜邦线是否松动

### OLED 没显示怎么办？

检查：

- OLED SCL 是否接 PB6
- OLED SDA 是否接 PB7
- OLED 地址是否为 0x3C
- OLED 是否接 3.3V 和 GND
- I2C 上拉是否正常

### 按键不灵敏怎么办？

检查：

- COM 是否接 GND
- 引脚是否按表接线
- 按下时对应 GPIO 是否变低
- 消抖时间是否过长
- 主循环里是否加入了长时间 `delay`

### 串口看不到输出怎么办？

检查：

- USB-TTL TXD 接 PA10
- USB-TTL RXD 接 PA9
- GND 共地
- 波特率 115200
- 数据格式 8N1
- USB-TTL 是否为 3.3V TTL 电平

## 9. 可以继续扩展的问题

### 是否需要 MPU6050 角度报警？

可以增加。

例如：

- Roll 超过 45 度显示 `TILT`
- Pitch 超过 45 度显示 `FALL`
- 串口输出报警状态

### 是否需要校准菜单？

可以增加。

在 `MPU6050` 页面增加按键功能：

- 右键：重新校准
- 按压：返回

但重新校准期间必须保持模块静止。

### 是否需要保存阈值？

当前阈值只保存在 RAM，断电后恢复默认值。

如果需要断电保存，可以后续增加 Flash 存储。

### 是否需要把 MPU6050 原始数据也显示出来？

可以增加，但 OLED 屏幕较小，更适合显示 Roll/Pitch/Yaw。

原始数据更适合通过串口 `STATUS` 或新增 `MPU RAW` 命令输出。

### 是否需要把 OLED 页面做成更多层级？

当前页面数量已经够用。

如果继续增加功能，可以改成：

- 主菜单
- 传感器
- 控制
- 设置
- 调试

这样页面结构会更清晰。
